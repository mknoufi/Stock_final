## 2024-05-24 - [List Rendering Optimization in React Native]
**Learning:** React Native's standard `FlatList` can suffer significant frame drops and memory issues when rendering long, complex items or paginated lists with infinite scrolling (like search results). The `VirtualList` component (which wraps `@shopify/flash-list`) is vastly superior for these use cases but requires a precisely calculated `estimatedItemSize` to function optimally.
**Action:** When working with potentially long lists in this codebase (especially in search or data tables), always prefer `VirtualList` over `FlatList`. Ensure you calculate an accurate `estimatedItemSize` by inspecting the item's layout and styles (padding, margins, font sizes) rather than guessing.## 2024-06-27 - Idempotent Batch Updates Count Discrepancies
**Learning:** In MongoDB Motor operations (specifically `update_many`), using `result.modified_count` to track success for idempotent updates (like bulk closing already closed sessions) can underreport actual processed records compared to iterating and tracking explicitly. When performing such batch updates, calculating `len(validated_input_array)` provides the accurate count of items processed correctly since already-updated records shouldn't count as failures.
**Action:** When migrating N+1 loops containing idempotent `update_one` database operations into a single O(1) `update_many` call via `$in`, explicitly set the return metrics (like `updated_count`) using `len(input_array)` rather than relying on `result.modified_count` to preserve accurate output messages.
