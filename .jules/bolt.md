## 2024-05-24 - [List Rendering Optimization in React Native]
**Learning:** React Native's standard `FlatList` can suffer significant frame drops and memory issues when rendering long, complex items or paginated lists with infinite scrolling (like search results). The `VirtualList` component (which wraps `@shopify/flash-list`) is vastly superior for these use cases but requires a precisely calculated `estimatedItemSize` to function optimally.
**Action:** When working with potentially long lists in this codebase (especially in search or data tables), always prefer `VirtualList` over `FlatList`. Ensure you calculate an accurate `estimatedItemSize` by inspecting the item's layout and styles (padding, margins, font sizes) rather than guessing.
## 2024-05-25 - [VirtualList Generic Types & forwardRef Compatibility]
**Learning:** When using generic components wrapped with `React.forwardRef` (like `VirtualList<T>`), TypeScript often struggles to infer the generic type alongside the ref. Casting the exported component (e.g., `as <T>(props: VirtualListProps<T> & { ref?: React.Ref<any> }) => React.ReactElement`) allows passing `ref` down to `FlashList` without breaking generic typing on the parent side.
**Action:** Always wrap the `forwardRef` of generic React Native components with a typed casting export to prevent TS errors on refs, and initialize refs in parent components using generic `any` or explicit component types when targeting custom wrapped components.

## 2024-05-25 - [VirtualList Generic Types & forwardRef Compatibility]
**Learning:** When using generic components wrapped with `React.forwardRef` (like `VirtualList<T>`), TypeScript often struggles to infer the generic type alongside the ref. Casting the exported component (e.g., `as (<T>(props: VirtualListProps<T> & { ref?: React.Ref<any> }) => React.ReactElement) & { displayName?: string }`) allows passing `ref` down to `FlashList` without breaking generic typing on the parent side, while also allowing static properties like `displayName`.
**Action:** Always wrap the `forwardRef` of generic React Native components with a typed casting export to prevent TS errors on refs, and initialize refs in parent components using generic `any` or explicit component types when targeting custom wrapped components.
