## 2024-05-24 - [List Rendering Optimization in React Native]
**Learning:** React Native's standard `FlatList` can suffer significant frame drops and memory issues when rendering long, complex items or paginated lists with infinite scrolling (like search results). The `VirtualList` component (which wraps `@shopify/flash-list`) is vastly superior for these use cases but requires a precisely calculated `estimatedItemSize` to function optimally.
**Action:** When working with potentially long lists in this codebase (especially in search or data tables), always prefer `VirtualList` over `FlatList`. Ensure you calculate an accurate `estimatedItemSize` by inspecting the item's layout and styles (padding, margins, font sizes) rather than guessing.## 2024-07-31 - [N+1 Optimization in Aggregation Loops]
**Learning:** In the Python backend (`dashboard_analytics_api.py`), analytic endpoints frequently iterated over grouped data (by session, category, location) and individually queried the database for item prices and count lines within the loops, creating significant N+1 query latency.
**Action:** Always batch fetch related records outside of loops using `$in` operators (after deduplicating IDs) or bounded range queries (like dates). Build in-memory dictionaries keyed by the parent ID (e.g., `item_code` or `session_id`) to enable O(1) lookups during the iteration step. Always include a safety check (e.g., `if all_item_codes:`) before executing `$in` queries to prevent MongoDB errors when passing empty arrays.
