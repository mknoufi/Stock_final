## 2024-05-24 - [List Rendering Optimization in React Native]
**Learning:** React Native's standard `FlatList` can suffer significant frame drops and memory issues when rendering long, complex items or paginated lists with infinite scrolling (like search results). The `VirtualList` component (which wraps `@shopify/flash-list`) is vastly superior for these use cases but requires a precisely calculated `estimatedItemSize` to function optimally.
**Action:** When working with potentially long lists in this codebase (especially in search or data tables), always prefer `VirtualList` over `FlatList`. Ensure you calculate an accurate `estimatedItemSize` by inspecting the item's layout and styles (padding, margins, font sizes) rather than guessing.## 2026-06-20 - N+1 Query in Item Serial Validation
**Learning:** The `sync_batch_api.py` endpoint `validate_record` had an N+1 query issue when checking for duplicate serial numbers within a single offline record submission. A naive implementation iterating `record.serial_numbers` resulted in `N` distinct database calls, causing potential bottlenecks under heavy synchronization loads.
**Action:** Replace `for serial in record.serial_numbers: db.item_serials.find_one()` with a batched MongoDB query using the `$in` operator: `db.item_serials.find({"serial_number": {"$in": serials_to_check}})` and building an in-memory dictionary for O(1) lookup. When writing unit tests with `MagicMock` for Motor operations involving `.to_list()`, ensure the mock cursor specifically defines an asynchronous `to_list` method (e.g., `cursor_mock.to_list = AsyncMock(return_value=[])`) instead of deeply nesting `AsyncMock` to avoid `IndentationError` or `object MagicMock can't be used in 'await' expression` exceptions during mock traversal.
