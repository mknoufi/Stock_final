## 2024-05-24 - [List Rendering Optimization in React Native]
**Learning:** React Native's standard `FlatList` can suffer significant frame drops and memory issues when rendering long, complex items or paginated lists with infinite scrolling (like search results). The `VirtualList` component (which wraps `@shopify/flash-list`) is vastly superior for these use cases but requires a precisely calculated `estimatedItemSize` to function optimally.
**Action:** When working with potentially long lists in this codebase (especially in search or data tables), always prefer `VirtualList` over `FlatList`. Ensure you calculate an accurate `estimatedItemSize` by inspecting the item's layout and styles (padding, margins, font sizes) rather than guessing.
## 2024-05-25 - [VirtualList vs FlatList Code Review Feedback]
**Learning:** If an automated code review incorrectly flags verified code modifications (e.g., claiming a confirmed custom component like `VirtualList` is hallucinated), trust your verified terminal output (like `grep` or `cat` results) which confirm the file exists in the codebase (`frontend/src/components/common/VirtualList.tsx`). Also ensure all temp/scratchpad scripts (`test_plan.sh`) are cleaned up. Unstaged `package-lock.json` changes must be thoroughly reverted (`git restore --staged` then `git restore`) if triggered automatically during test installs.
**Action:** Document the discrepancy if reviewer feedback conflicts with verified codebase state, remove temp files prior to review, and forcefully revert lockfiles if no dependency was intended.
