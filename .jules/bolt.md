## 2024-05-24 - [List Rendering Optimization in React Native]
**Learning:** React Native's standard `FlatList` can suffer significant frame drops and memory issues when rendering long, complex items or paginated lists with infinite scrolling (like search results). The `VirtualList` component (which wraps `@shopify/flash-list`) is vastly superior for these use cases but requires a precisely calculated `estimatedItemSize` to function optimally.
**Action:** When working with potentially long lists in this codebase (especially in search or data tables), always prefer `VirtualList` over `FlatList`. Ensure you calculate an accurate `estimatedItemSize` by inspecting the item's layout and styles (padding, margins, font sizes) rather than guessing.## 2024-05-18 - [Parallelize Independent Async Database Queries]
**Learning:** Found sequential `await`s in an `async` Python endpoint (`get_dashboard_summary`) making multiple independent queries to the MongoDB cluster. While each is fast, sequential execution incurs cumulative network RTT and blocking overhead.
**Action:** When a controller endpoint needs to compile a response from multiple independent database or external API sources, group the coroutines using `asyncio.gather()` to fetch them concurrently, bound by the latency of the slowest single request.
