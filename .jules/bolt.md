## 2024-05-24 - [List Rendering Optimization in React Native]
**Learning:** React Native's standard `FlatList` can suffer significant frame drops and memory issues when rendering long, complex items or paginated lists with infinite scrolling (like search results). The `VirtualList` component (which wraps `@shopify/flash-list`) is vastly superior for these use cases but requires a precisely calculated `estimatedItemSize` to function optimally.
**Action:** When working with potentially long lists in this codebase (especially in search or data tables), always prefer `VirtualList` over `FlatList`. Ensure you calculate an accurate `estimatedItemSize` by inspecting the item's layout and styles (padding, margins, font sizes) rather than guessing.## 2024-07-27 - [Optimize SearchAutocomplete]
**Learning:** Found that `<SearchAutocomplete>` still used `FlatList` despite other components using `VirtualList`. When forwarding a ref with `VirtualList` (which wraps `FlashList`), the generic types can be tricky. It requires wrapping the component in `forwardRef` and casting the final export to handle generic types correctly (`as <T>(props: VirtualListProps<T> & { ref?: React.Ref<any> }) => React.ReactElement`).
**Action:** Use `VirtualList` instead of `FlatList` for all long list components. Make sure `VirtualList` is using `forwardRef` and is typed properly when it needs to accept a ref from parent components.
