## 2024-05-24 - [List Rendering Optimization in React Native]
**Learning:** React Native's standard `FlatList` can suffer significant frame drops and memory issues when rendering long, complex items or paginated lists with infinite scrolling (like search results). The `VirtualList` component (which wraps `@shopify/flash-list`) is vastly superior for these use cases but requires a precisely calculated `estimatedItemSize` to function optimally.
**Action:** When working with potentially long lists in this codebase (especially in search or data tables), always prefer `VirtualList` over `FlatList`. Ensure you calculate an accurate `estimatedItemSize` by inspecting the item's layout and styles (padding, margins, font sizes) rather than guessing.## 2024-06-01 - [Resolving N+1 Query in Breakdown by Session]
**Learning:** Nested list comprehensions iterating over bulk MongoDB aggregates inside Python can be a bottleneck. Performing repeated asynchronous `await db.find` calls within python loops triggers severe N+1 latency issues resulting in long response times.
**Action:** Replace looped `db.find` calls spanning multiple entities with a single `$in` query batch fetching the entire result set, and then map them locally to reconstruct the desired relations (like session to count lines, or item to price map) to ensure fast lookup operations.
