## 2024-05-24 - [List Rendering Optimization in React Native]
**Learning:** React Native's standard `FlatList` can suffer significant frame drops and memory issues when rendering long, complex items or paginated lists with infinite scrolling (like search results). The `VirtualList` component (which wraps `@shopify/flash-list`) is vastly superior for these use cases but requires a precisely calculated `estimatedItemSize` to function optimally.
**Action:** When working with potentially long lists in this codebase (especially in search or data tables), always prefer `VirtualList` over `FlatList`. Ensure you calculate an accurate `estimatedItemSize` by inspecting the item's layout and styles (padding, margins, font sizes) rather than guessing.## 2024-05-18 - [Replicating Python Falsy Evaluation in MongoDB `$cond`]
**Learning:** In MongoDB aggregation pipelines, numeric `0` evaluates to truthy, unlike in standard Python falsy checks. When trying to replicate Python fallback logic like `mrp_counted or mrp_erp` for numeric fields, simply using `$cond` or `$ifNull` is insufficient and buggy because an explicit `0` value will trigger the truthy branch.
**Action:** When replicating Python fallback logic for numbers, use an explicit combination of `$type` matching (e.g., `["double", "int", "long", "decimal"]`) and `$ne: 0` checks within a `$cond` block to correctly bypass zero, null, and empty string values.
