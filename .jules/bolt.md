## 2024-05-24 - [List Rendering Optimization in React Native]
**Learning:** React Native's standard `FlatList` can suffer significant frame drops and memory issues when rendering long, complex items or paginated lists with infinite scrolling (like search results). The `VirtualList` component (which wraps `@shopify/flash-list`) is vastly superior for these use cases but requires a precisely calculated `estimatedItemSize` to function optimally.
**Action:** When working with potentially long lists in this codebase (especially in search or data tables), always prefer `VirtualList` over `FlatList`. Ensure you calculate an accurate `estimatedItemSize` by inspecting the item's layout and styles (padding, margins, font sizes) rather than guessing.## 2024-03-24 - VirtualList vs FlatList sizing
**Learning:** When migrating from `FlatList` to `VirtualList` (FlashList wrapper) in React Native, the required `estimatedItemSize` must be calculated accurately based on the component's styling tokens (e.g., modernSpacing, modernTypography) to prevent layout jumps or performance degradation. `VirtualList` will throw warnings or fail to render efficiently if `estimatedItemSize` is omitted or wildly inaccurate.
**Action:** Always verify exact style token values in the `StyleSheet` and corresponding theme file (e.g., `modernDesignSystem.ts`) to compute the layout height (padding + line height + borders) before adding `estimatedItemSize`.
