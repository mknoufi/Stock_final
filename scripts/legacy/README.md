# Legacy Scripts

This directory is archived reference material.

These scripts are not part of the supported development, runtime, or deployment path for this repository. They may reference old ports, old frontend/admin-panel flows, or pre-canonical startup assumptions.

Use the root `Makefile`, `docker-compose.production.yml`, and the non-legacy scripts under [`scripts/`](/Users/noufi1/stk_final/Stock_final/scripts) for current operations.
